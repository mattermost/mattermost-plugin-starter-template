checks:
  - type: repo_is_clean
    params:
      repo: template
  - type: repo_is_clean
    params:
      repo: plugin
actions:
  - path: go.mod
    actions:
      first_succesful:
        - do: overwrite
          conditions:
            - type: exists
            - type: in_repo
              params:
                repo: source
        - do: merge_gomod
  - path: go.sum
    action:
      
